import { Center, ScrollView, Text } from "native-base";
import React from "react";

const Lois = () => {

    return (
        <ScrollView>
            <Center flex={1} p="4">
                <Text>Lois</Text>
            </Center>
        </ScrollView>
    )
}

export default Lois;